import 'package:flutter/material.dart';

class ChargingActivity extends StatelessWidget {
  const ChargingActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Charging activity screen content"),
    );
  }
}
