import 'package:flutter/widgets.dart';

class DistanceContent extends StatelessWidget {
  const DistanceContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Distance content",
      ),
    );
  }
}
